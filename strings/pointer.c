#include<stdio.h>
#include<string.h>
int main()
{
char str[]="piyus loves prakshi";
char  *ptr="hello"; //pointer ptr stores starting addrress of string
printf("%s\n",ptr);
printf("%s",str);
    return 0;
}